# Desktop tutorial audit — 2026-09-22

## Scope

Compared the previous tutorial release, `7d8dbab5`, with `806b191a`:
24 commits affecting Panel or Desktop, 93 changed files. Existing uncommitted
workspace edits were retained; this update does not change backend contracts,
vendor code, or business data.

| Surface                | Relevant change                                                              | Tutorial response                                                                                                          |
| ---------------------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Agent Workbench        | Split from manual queues; shared creation-time filter                        | Seven steps, three Agent tabs, independent time explanation, return to pending review                                      |
| Manual Workbench       | New route for samples and creator messages                                   | Six steps, page introduction, separate queue/time explanations, return to samples                                          |
| Product Knowledge      | Rich-text/Markdown paste, image/video uploads, multi-SKU discovery           | Five steps; open an existing record read-only, explain media and bindings, close only the detail opened by the tour        |
| Team & channels        | Guided spreadsheet template, existing-tag validation, seller UID/note fields | Explain template instructions, text-format UIDs, existing tags, preview and overwrite effects; initialize the tour on Team |
| Creators               | Seller-provided UID and relationship note                                    | Explain header UID, Management notes, explicit save and bulk-update entry point                                            |
| Campaigns              | Target-field validation and numeric-range labels                             | Explain invalid target fields and distinguish qualification, delivery and response evidence                                |
| Affiliate Intelligence | Searchable shop scope                                                        | Explain name/alias/ID search and scope selection                                                                           |
| Shops                  | Japan onboarding market; detail navigation                                   | Mention Japan and target the visible service tabs in the detail tour                                                       |

Other audited changes include searchable shop selectors in billing and related
pages, localized enum labels, Desktop MCP authorization handoff, subscription
token-refresh handling, and customer-service archive identity. Existing page
tours do not require new steps for these changes. MCP handoff is a deep-link
flow rather than a new Panel page; the tour does not initiate authorization.

## Infrastructure findings from visual QA

- Final runtime locale merging could override refreshed tutorial copy with the
  escalation bundle. Merge it before tutorial overrides, and test the actual
  `LANGUAGE_RESOURCES`, not just intermediate language options.
- Async queues can replace a loading target or grow after initial measurement.
  Follow target replacement, resizing, and sibling-driven layout shifts when
  positioning the spotlight. Do not rerun step preparation during repositioning.
- Use the shop drawer's top service tabs as the tutorial navigation target.

All new and changed copy is supplied in English, Chinese, German, Spanish,
French, Indonesian, Italian, and Thai. The new bundle enforces matching key
shapes at compile time and verifies final runtime values in tests.

## Verification

- Panel: 126 test files / 1,229 tests passed.
- TypeScript: `tsc --noEmit` passed.
- Lint, architecture, CSS usage and CSS graph checks passed. Two unrelated
  unused-import warnings remain; no lint errors.
- Production Panel build passed, with the existing large-chunk warning.
- Computer Use on the running Electron Desktop: all eight affected routes
  traversed in Chinese/light and English/dark. Inspected the updated spotlight,
  copy, time controls, and detail/wizard transitions.
- Verified Agent/Manual/Team default-tab restoration, unsaved campaign wizard
  closure, Product Knowledge completion/back/early-exit cleanup, and automatic
  skipping of detail steps for an empty search result. Restored the original
  Chinese/light settings and cleared the temporary search afterward.
- No sample approval, message sending, imports, uploads, record creation,
  saves, linking, or unlinking were performed during visual QA. Other six
  languages received automated coverage checks, not native-language visual QA.

The existing Team template assertion was updated for `creator_uid_note` and
`creator_note`, matching the current import schema.
