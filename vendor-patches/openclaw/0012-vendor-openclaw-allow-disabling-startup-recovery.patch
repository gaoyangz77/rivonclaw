From 977c1f2d7a7b1a7422a07c48288c3a528fc59ac7 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Sat, 8 Aug 2026 05:00:58 -0700
Subject: [PATCH] vendor(openclaw): allow disabling startup recovery

Why:
- RivonClaw receives customer-service retries from backend/Airflow rather than relying on local gateway replay.
- OpenClaw startup delivery recovery and interrupted-session recovery can overwhelm the single desktop gateway process when many customer-service deliveries or aborted WeChat sessions are queued.

Removal:
- Drop when upstream OpenClaw exposes first-class config for startup recovery policies or makes startup recovery budgeted enough for RivonClaw customer-service workloads.

Tests:
- vendor/openclaw/src/gateway/server-runtime-services.test.ts
- vendor/openclaw/src/gateway/server-startup-post-attach.test.ts
- apps/desktop/src/infra/proxy/proxy-manager.test.ts
---
 src/gateway/server-runtime-services.test.ts   | 17 +++++++++
 src/gateway/server-runtime-services.ts        | 36 +++++++++++++------
 src/gateway/server-startup-finish.ts          |  1 +
 .../server-startup-post-attach.test.ts        |  9 +++++
 src/gateway/server-startup-post-attach.ts     | 13 ++++++-
 5 files changed, 65 insertions(+), 11 deletions(-)

diff --git a/src/gateway/server-runtime-services.test.ts b/src/gateway/server-runtime-services.test.ts
index ed75691a351..3ff860e927c 100644
--- a/src/gateway/server-runtime-services.test.ts
+++ b/src/gateway/server-runtime-services.test.ts
@@ -497,6 +497,23 @@ describe("server-runtime-services", () => {
     );
   });
 
+  it("can disable startup delivery and session restart recovery by environment", async () => {
+    vi.useFakeTimers();
+    activateScheduledServicesForTest({
+      env: {
+        OPENCLAW_DISABLE_OUTBOUND_DELIVERY_RECOVERY: "1",
+        OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY: "1",
+      } as NodeJS.ProcessEnv,
+    });
+
+    await vi.advanceTimersByTimeAsync(1_250);
+    await vi.dynamicImportSettled();
+
+    expect(hoisted.recoverPendingDeliveries).not.toHaveBeenCalled();
+    expect(hoisted.recoverPendingRestartContinuationDeliveries).not.toHaveBeenCalled();
+    expect(hoisted.schedulePendingSessionDeliveries).not.toHaveBeenCalled();
+  });
+
   it("can defer cron startup while activating other scheduled services", async () => {
     vi.useFakeTimers();
     const cron = { start: vi.fn(async () => undefined) };
diff --git a/src/gateway/server-runtime-services.ts b/src/gateway/server-runtime-services.ts
index 6d34c432e65..8027d5364bb 100644
--- a/src/gateway/server-runtime-services.ts
+++ b/src/gateway/server-runtime-services.ts
@@ -3,6 +3,7 @@
 import { getRuntimeConfig } from "../config/config.js";
 import type { OpenClawConfig } from "../config/types.openclaw.js";
 import { computeBackoffMs } from "../infra/delivery-recovery.shared.js";
+import { isTruthyEnvValue } from "../infra/env.js";
 import {
   resolveHeartbeatAgents,
   startHeartbeatRunner,
@@ -187,7 +188,12 @@ const RECOVERY_SHUTDOWN_STILL_PENDING_WARN_MS = 5_000;
 function startPendingOutboundDeliveryRecovery(params: {
   cfg: OpenClawConfig;
   log: GatewayRuntimeServiceLogger;
+  env: NodeJS.ProcessEnv;
 }): () => Promise<void> {
+  if (isTruthyEnvValue(params.env.OPENCLAW_DISABLE_OUTBOUND_DELIVERY_RECOVERY)) {
+    params.log.child("delivery-recovery").info("disabled by environment");
+    return async () => {};
+  }
   let stopped = false;
   let inFlight: Promise<void> | null = null;
   let stopPromise: Promise<void> | null = null;
@@ -270,6 +276,7 @@ function startPendingSessionDeliveryRuntime(params: {
   deps: import("../cli/deps.types.js").CliDeps;
   log: GatewayRuntimeServiceLogger;
   maxEnqueuedAt: number;
+  disableRestartRecovery: boolean;
 }): () => void {
   let stopped = false;
   let stopRuntime: (() => void) | undefined;
@@ -296,16 +303,20 @@ function startPendingSessionDeliveryRuntime(params: {
         log: logRecovery,
         onSettled: settleQueuedSessionDelivery,
       });
-      try {
-        await recoverPendingRestartContinuationDeliveries({
-          deps: params.deps,
-          log: logRecovery,
-          maxEnqueuedAt: params.maxEnqueuedAt,
-        });
-      } finally {
-        // Recovery and scheduling are independent safeguards. A transient
-        // recovery failure must not leave persisted rows without timers.
-        await schedulePendingSessionDeliveries();
+      if (params.disableRestartRecovery) {
+        logRecovery.info("restart recovery disabled by environment");
+      } else {
+        try {
+          await recoverPendingRestartContinuationDeliveries({
+            deps: params.deps,
+            log: logRecovery,
+            maxEnqueuedAt: params.maxEnqueuedAt,
+          });
+        } finally {
+          // Recovery and scheduling are independent safeguards. A transient
+          // recovery failure must not leave persisted rows without timers.
+          await schedulePendingSessionDeliveries();
+        }
       }
     }).catch((err: unknown) =>
       params.log.error(`Session delivery recovery failed: ${String(err)}`),
@@ -331,6 +342,7 @@ export function activateGatewayScheduledServices(params: {
   startCron?: boolean;
   logCron: { error: (message: string) => void };
   log: GatewayRuntimeServiceLogger;
+  env?: NodeJS.ProcessEnv;
 }): { heartbeatRunner: HeartbeatRunner; stopOutboundDeliveryRecovery: () => Promise<void> } {
   if (params.minimalTestGateway) {
     // Minimal gateways keep handles callable but inert so tests can share shutdown paths with
@@ -361,6 +373,9 @@ export function activateGatewayScheduledServices(params: {
     deps: params.deps,
     log: params.log,
     maxEnqueuedAt: params.sessionDeliveryRecoveryMaxEnqueuedAt,
+    disableRestartRecovery: isTruthyEnvValue(
+      (params.env ?? process.env).OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY,
+    ),
   });
   if (params.startCron !== false) {
     startGatewayCronWithLogging({
@@ -374,6 +389,7 @@ export function activateGatewayScheduledServices(params: {
   const stopOutboundDeliveryRecovery = startPendingOutboundDeliveryRecovery({
     cfg: params.cfgAtStart,
     log: params.log,
+    env: params.env ?? process.env,
   });
   const heartbeatRunnerWithUpstreamMonitor: HeartbeatRunner = {
     updateConfig: heartbeatRunner.updateConfig,
diff --git a/src/gateway/server-startup-finish.ts b/src/gateway/server-startup-finish.ts
index a6e5868fc0b..dda6aa3b8dd 100644
--- a/src/gateway/server-startup-finish.ts
+++ b/src/gateway/server-startup-finish.ts
@@ -402,6 +402,7 @@ export async function finishGatewayStartup(params: {
         startCron: false,
         logCron,
         log,
+        env: process.env,
       });
       runtimeState.heartbeatRunner = activated.heartbeatRunner;
       runtimeState.stopOutboundDeliveryRecovery = activated.stopOutboundDeliveryRecovery;
diff --git a/src/gateway/server-startup-post-attach.test.ts b/src/gateway/server-startup-post-attach.test.ts
index 563049f76ce..4e92c59f194 100644
--- a/src/gateway/server-startup-post-attach.test.ts
+++ b/src/gateway/server-startup-post-attach.test.ts
@@ -507,6 +507,15 @@ describe("startGatewayPostAttachRuntime", () => {
     await cleanupGatewayTestState();
   });
 
+  it("recognizes the desktop session recovery opt-out", () => {
+    expect(
+      testing.shouldDisableSessionRestartRecovery({
+        OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY: "1",
+      } as NodeJS.ProcessEnv),
+    ).toBe(true);
+    expect(testing.shouldDisableSessionRestartRecovery({} as NodeJS.ProcessEnv)).toBe(false);
+  });
+
   it("drains tracked sidecars and resets fixture state after the first cleanup failure", async () => {
     const firstError = new Error("first cleanup failure");
     const stopOrder: string[] = [];
diff --git a/src/gateway/server-startup-post-attach.ts b/src/gateway/server-startup-post-attach.ts
index 646ef15130f..ce1c39c782f 100644
--- a/src/gateway/server-startup-post-attach.ts
+++ b/src/gateway/server-startup-post-attach.ts
@@ -56,8 +56,13 @@ const AGENT_RUNTIME_PLUGIN_PREWARM_START_DELAY_MS = 0;
 const AGENT_RUNTIME_PLUGIN_PREWARM_RETRY_DELAY_MS = 250;
 const DEFERRED_SIDECAR_START_DELAY_MS = 100;
 const SKIP_STARTUP_MODEL_PREWARM_ENV = "OPENCLAW_SKIP_STARTUP_MODEL_PREWARM";
+const DISABLE_SESSION_RESTART_RECOVERY_ENV = "OPENCLAW_DISABLE_SESSION_RESTART_RECOVERY";
 type Awaitable<T> = T | Promise<T>;
 
+function shouldDisableSessionRestartRecovery(env: NodeJS.ProcessEnv = process.env): boolean {
+  return isTruthyEnvValue(env[DISABLE_SESSION_RESTART_RECOVERY_ENV]);
+}
+
 type GatewayMemoryStartupPolicy =
   | { mode: "off" }
   | { mode: "immediate" }
@@ -678,6 +683,9 @@ export async function startGatewaySidecars(params: {
   // These runs were orphaned by the previous Gateway lifecycle. Record that fact
   // even if this process later fails model preparation and never starts channels.
   await measureStartup(params.startupTrace, "sidecars.main-session-recovery", async () => {
+    if (shouldDisableSessionRestartRecovery()) {
+      return;
+    }
     try {
       const { markStartupOrphanedMainSessionsForRecovery } = await measureStartup(
         params.startupTrace,
@@ -1338,7 +1346,9 @@ export async function startGatewayPostAttachRuntime(
         ]);
         let mainSessionRecoverySidecar: GatewayPostReadySidecarHandle | undefined;
         try {
-          if (params.isClosing?.() !== true) {
+          if (shouldDisableSessionRestartRecovery()) {
+            params.log.info("main-session restart recovery disabled by environment");
+          } else if (params.isClosing?.() !== true) {
             const { scheduleRestartAbortedMainSessionRecovery } =
               await loadMainSessionRestartRecoveryModule();
             // Closing can begin while the runtime module is loading; a late owner
@@ -1511,6 +1521,7 @@ export const testing = {
   publishStartupModelRuntime,
   refreshLatestUpdateRestartSentinelIfPresent,
   resolveGatewayMemoryStartupPolicy,
+  shouldDisableSessionRestartRecovery,
   scheduleProviderAuthStatePrewarm,
   scheduleRestartSentinelWakeAfterReady,
   shouldSkipStartupModelPrewarm,
-- 
2.50.1 (Apple Git-155)

