From e230c11b862900cfc9b88e008780e2b963c15009 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Wed, 3 Jun 2026 22:09:15 -0700
Subject: [PATCH] vendor(openclaw): route remote media through RivonClaw cache

Why:
- RivonClaw Desktop users on the CN relay route cannot reliably fetch GFW-blocked third-party media URLs such as TikTok Shop image/video assets from agent media tools.
- Desktop owns the authenticated media-cache endpoint and first-party route decision, so OpenClaw should resolve remote media through that local bridge before its normal guarded fetch.

Removal:
- Drop when upstream OpenClaw exposes a host-provided remote-media URL resolver hook that RivonClaw can configure without patching source.

Tests:
- src/media/fetch.test.ts
---
 src/media/fetch.test.ts | 70 +++++++++++++++++++++++++++++++++++++-
 src/media/fetch.ts      | 75 ++++++++++++++++++++++++++++++++++++++++-
 2 files changed, 143 insertions(+), 2 deletions(-)

diff --git a/src/media/fetch.test.ts b/src/media/fetch.test.ts
index 7b91df5dd6..b7bb9d83b1 100644
--- a/src/media/fetch.test.ts
+++ b/src/media/fetch.test.ts
@@ -1,6 +1,6 @@
 // Media fetch tests cover remote media download limits and validation.
 import fs from "node:fs/promises";
-import { afterAll, beforeAll, beforeEach, describe, expect, it, vi } from "vitest";
+import { afterAll, afterEach, beforeAll, beforeEach, describe, expect, it, vi } from "vitest";
 import { MAX_TIMER_TIMEOUT_MS } from "../shared/number-coercion.js";
 import { createTempHomeEnv, type TempHomeEnv } from "../test-utils/temp-home.js";
 
@@ -26,6 +26,13 @@ let saveResponseMedia: SaveResponseMedia;
 let defaultFetchMediaMaxBytes: number;
 let tempHome: TempHomeEnv;
 
+function clearRivonClawMediaCacheEnv() {
+  delete process.env.RIVONCLAW_PANEL_PORT;
+  delete process.env.RIVONCLAW_DESKTOP_API_TOKEN;
+  delete process.env.RIVONCLAW_FIRST_PARTY_DOMAIN_ROUTE;
+  delete process.env.RIVONCLAW_CN_RELAY;
+}
+
 function makeStream(chunks: Uint8Array[]) {
   return new ReadableStream<Uint8Array>({
     start(controller) {
@@ -228,6 +235,7 @@ describe("readRemoteMediaBuffer", () => {
 
   beforeEach(() => {
     vi.useRealTimers();
+    clearRivonClawMediaCacheEnv();
     fetchWithSsrFGuardMock.mockReset().mockImplementation(async (paramsUnknown: unknown) => {
       const params = paramsUnknown as {
         url: string;
@@ -253,6 +261,66 @@ describe("readRemoteMediaBuffer", () => {
     await tempHome.restore();
   });
 
+  afterEach(() => {
+    clearRivonClawMediaCacheEnv();
+    vi.unstubAllGlobals();
+  });
+
+  it("resolves remote media through the RivonClaw Desktop media cache", async () => {
+    process.env.RIVONCLAW_PANEL_PORT = "61234";
+    process.env.RIVONCLAW_DESKTOP_API_TOKEN = "desktop-token";
+    process.env.RIVONCLAW_FIRST_PARTY_DOMAIN_ROUTE = "cn-relay";
+    vi.stubGlobal(
+      "fetch",
+      vi.fn(async () =>
+        new Response(JSON.stringify({ url: "https://media-cache.example.com/image.jpeg" }), {
+          status: 200,
+        }),
+      ),
+    );
+    await readRemoteMediaBuffer({
+      url: "https://blocked.example.com/image.jpeg",
+      fetchImpl: vi.fn(async () =>
+        new Response(new Uint8Array([0xff, 0xd8, 0xff, 0xd9]), { status: 200 }),
+      ),
+      lookupFn: makeLookupFn(),
+      maxBytes: 1024,
+    });
+    expect(fetchWithSsrFGuardMock).toHaveBeenCalledWith(
+      expect.objectContaining({ url: "https://media-cache.example.com/image.jpeg" }),
+    );
+  });
+
+  it("falls back to the original URL when the desktop cache is unavailable globally", async () => {
+    process.env.RIVONCLAW_PANEL_PORT = "61234";
+    process.env.RIVONCLAW_DESKTOP_API_TOKEN = "desktop-token";
+    process.env.RIVONCLAW_FIRST_PARTY_DOMAIN_ROUTE = "global";
+    vi.stubGlobal("fetch", vi.fn(async () => Promise.reject(new Error("unavailable"))));
+    await readRemoteMediaBuffer({
+      url: "https://example.com/image.jpeg",
+      fetchImpl: vi.fn(async () => new Response(new Uint8Array([1]), { status: 200 })),
+      lookupFn: makeLookupFn(),
+      maxBytes: 1024,
+    });
+    expect(fetchWithSsrFGuardMock).toHaveBeenCalledWith(
+      expect.objectContaining({ url: "https://example.com/image.jpeg" }),
+    );
+  });
+
+  it("fails clearly when the desktop cache is unavailable on the CN relay route", async () => {
+    process.env.RIVONCLAW_PANEL_PORT = "61234";
+    process.env.RIVONCLAW_DESKTOP_API_TOKEN = "desktop-token";
+    process.env.RIVONCLAW_FIRST_PARTY_DOMAIN_ROUTE = "cn-relay";
+    vi.stubGlobal("fetch", vi.fn(async () => new Response("denied", { status: 401 })));
+    await expect(
+      readRemoteMediaBuffer({
+        url: "https://example.com/image.jpeg",
+        fetchImpl: vi.fn(),
+        lookupFn: makeLookupFn(),
+      }),
+    ).rejects.toThrow("RivonClaw media cache proxy resolve failed");
+  });
+
   it.each([
     {
       name: "rejects when content-length exceeds maxBytes",
diff --git a/src/media/fetch.ts b/src/media/fetch.ts
index d2d10ee113..774c680e8d 100644
--- a/src/media/fetch.ts
+++ b/src/media/fetch.ts
@@ -67,6 +67,10 @@ export type FetchDispatcherAttempt = {
   lookupFn?: LookupFn;
 };
 
+type RivonClawMediaCacheResolveResponse = {
+  url?: unknown;
+};
+
 type FetchMediaOptions = {
   url: string;
   fetchImpl?: FetchLike;
@@ -179,6 +183,74 @@ function redactMediaUrl(url: string): string {
   return redactSensitiveText(url);
 }
 
+function shouldFailHardOnRivonClawMediaCacheError(): boolean {
+  return (
+    process.env.RIVONCLAW_FIRST_PARTY_DOMAIN_ROUTE === "cn-relay" ||
+    process.env.RIVONCLAW_CN_RELAY === "1"
+  );
+}
+
+function isLoopbackUrl(url: string): boolean {
+  try {
+    const parsed = new URL(url);
+    return ["localhost", "127.0.0.1", "::1", "[::1]"].includes(parsed.hostname);
+  } catch {
+    return false;
+  }
+}
+
+async function readResolveErrorSnippet(res: Response): Promise<string> {
+  try {
+    const text = await res.text();
+    return text ? `; body: ${redactSensitiveText(text.slice(0, 512))}` : "";
+  } catch {
+    return "";
+  }
+}
+
+async function resolveRivonClawDesktopMediaCacheUrl(url: string): Promise<string> {
+  if (!/^https?:\/\//i.test(url) || isLoopbackUrl(url)) {
+    return url;
+  }
+  const panelPort = process.env.RIVONCLAW_PANEL_PORT?.trim();
+  const desktopToken = process.env.RIVONCLAW_DESKTOP_API_TOKEN?.trim();
+  if (!panelPort || !desktopToken || !/^\d{2,5}$/.test(panelPort)) {
+    return url;
+  }
+  const controller = new AbortController();
+  const timeout = setTimeout(() => controller.abort(), 10_000);
+  timeout.unref?.();
+  try {
+    const res = await globalThis.fetch(`http://127.0.0.1:${panelPort}/api/media-cache/resolve`, {
+      method: "POST",
+      headers: {
+        "Content-Type": "application/json",
+        "X-RivonClaw-Desktop-Token": desktopToken,
+      },
+      body: JSON.stringify({ sourceUrl: url }),
+      signal: controller.signal,
+    });
+    if (!res.ok) {
+      throw new Error(`HTTP ${res.status}${await readResolveErrorSnippet(res)}`);
+    }
+    const body = (await res.json()) as RivonClawMediaCacheResolveResponse;
+    if (typeof body.url !== "string" || !/^https?:\/\//i.test(body.url)) {
+      throw new Error("response did not include a valid http(s) url");
+    }
+    return body.url;
+  } catch (err) {
+    if (!shouldFailHardOnRivonClawMediaCacheError()) {
+      return url;
+    }
+    throw new Error(
+      `RivonClaw media cache proxy resolve failed for ${redactMediaUrl(url)}: ${formatErrorMessage(err)}`,
+      { cause: err },
+    );
+  } finally {
+    clearTimeout(timeout);
+  }
+}
+
 async function fetchGuardedMediaResponse(
   options: FetchMediaOptions,
 ): Promise<GuardedMediaResponse> {
@@ -196,6 +268,7 @@ async function fetchGuardedMediaResponse(
     trustExplicitProxyDns,
   } = options;
   const sourceUrl = redactMediaUrl(url);
+  const requestUrl = await resolveRivonClawDesktopMediaCacheUrl(url);
 
   // Dispatcher attempts are fallback routes inside one logical guarded fetch operation.
   const attempts =
@@ -207,7 +280,7 @@ async function fetchGuardedMediaResponse(
       (trustExplicitProxyDns && attempt.dispatcherPolicy?.mode === "explicit-proxy"
         ? withTrustedExplicitProxyGuardedFetchMode
         : withStrictGuardedFetchMode)({
-        url,
+        url: requestUrl,
         fetchImpl,
         init: requestInit,
         maxRedirects,
-- 
2.50.1 (Apple Git-155)

