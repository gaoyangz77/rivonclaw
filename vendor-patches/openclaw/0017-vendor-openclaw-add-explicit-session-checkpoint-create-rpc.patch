From 105f0630e6c9792fec1c34d2252054d16d9cfce1 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Sat, 8 Aug 2026 20:25:45 -0700
Subject: [PATCH] vendor(openclaw): add explicit session checkpoint create rpc

Why:
- Affiliate checkpoint-based session management needs a preselected candidate checkpoint id so backend proposals can reference the exact transcript boundary before approval.

Removal:
- Drop when upstream exposes a business-neutral session checkpoint creation RPC that accepts a caller-provided checkpoint id and can be restored by checkpoint id.

Tests:
- src/gateway/server.sessions.compaction.test.ts
- apps/desktop/src/affiliate/affiliate-work-item.test.ts
---
 ...ocol-schema-fragment-sessions-lifecycle.ts |  2 +
 .../gateway-protocol/src/schema/sessions.ts   | 15 +++++
 .../src/validator-registry.ts                 |  3 +
 src/gateway/methods/core-descriptors.ts       |  1 +
 .../sessions-compaction-checkpoints.ts        | 63 ++++++++++++++++++-
 .../server.sessions.compaction.test.ts        | 57 +++++++++++++++++
 src/gateway/session-compaction-checkpoints.ts | 61 ++++++++++++++++--
 7 files changed, 196 insertions(+), 6 deletions(-)

diff --git a/packages/gateway-protocol/src/schema/protocol-schema-fragment-sessions-lifecycle.ts b/packages/gateway-protocol/src/schema/protocol-schema-fragment-sessions-lifecycle.ts
index 4a32cea02b0..deec1dfe4e2 100644
--- a/packages/gateway-protocol/src/schema/protocol-schema-fragment-sessions-lifecycle.ts
+++ b/packages/gateway-protocol/src/schema/protocol-schema-fragment-sessions-lifecycle.ts
@@ -6,10 +6,12 @@ export const SessionLifecycleProtocolSchemas = {
   SessionsCompactionGetParams: sessions.SessionsCompactionGetParamsSchema,
   SessionsCompactionBranchParams: sessions.SessionsCompactionBranchParamsSchema,
   SessionsCompactionRestoreParams: sessions.SessionsCompactionRestoreParamsSchema,
+  SessionsCheckpointCreateParams: sessions.SessionsCheckpointCreateParamsSchema,
   SessionsCompactionListResult: sessions.SessionsCompactionListResultSchema,
   SessionsCompactionGetResult: sessions.SessionsCompactionGetResultSchema,
   SessionsCompactionBranchResult: sessions.SessionsCompactionBranchResultSchema,
   SessionsCompactionRestoreResult: sessions.SessionsCompactionRestoreResultSchema,
+  SessionsCheckpointCreateResult: sessions.SessionsCheckpointCreateResultSchema,
   SessionsRewindParams: sessions.SessionsRewindParamsSchema,
   SessionsRewindResult: sessions.SessionsRewindResultSchema,
   SessionsForkParams: sessions.SessionsForkParamsSchema,
diff --git a/packages/gateway-protocol/src/schema/sessions.ts b/packages/gateway-protocol/src/schema/sessions.ts
index f4d3c9cc93b..c55ae150bb7 100644
--- a/packages/gateway-protocol/src/schema/sessions.ts
+++ b/packages/gateway-protocol/src/schema/sessions.ts
@@ -733,6 +733,12 @@ export const SessionsBranchesSwitchParamsSchema = closedObject({
 
 export const SessionsBranchesSwitchResultSchema = closedObject({});
 
+export const SessionsCheckpointCreateParamsSchema = closedObject({
+  key: NonEmptyString,
+  checkpointId: NonEmptyString,
+  summary: Type.Optional(Type.String()),
+});
+
 /** List response for session compaction checkpoints. */
 export const SessionsCompactionListResultSchema = closedObject({
   ok: Type.Literal(true),
@@ -778,6 +784,13 @@ export const SessionsCompactionRestoreResultSchema = closedObject({
   ),
 });
 
+export const SessionsCheckpointCreateResultSchema = closedObject({
+  ok: Type.Literal(true),
+  key: NonEmptyString,
+  sessionId: NonEmptyString,
+  checkpoint: SessionCompactionCheckpointSchema,
+});
+
 /** Usage report query across one session, one agent, or all agent sessions. */
 export const SessionsUsageParamsSchema = closedObject({
   /** Specific session key to analyze; if omitted returns sessions for the effective agent. */
@@ -861,10 +874,12 @@ export type SessionsCompactionListParams = Static<typeof SessionsCompactionListP
 export type SessionsCompactionGetParams = Static<typeof SessionsCompactionGetParamsSchema>;
 export type SessionsCompactionBranchParams = Static<typeof SessionsCompactionBranchParamsSchema>;
 export type SessionsCompactionRestoreParams = Static<typeof SessionsCompactionRestoreParamsSchema>;
+export type SessionsCheckpointCreateParams = Static<typeof SessionsCheckpointCreateParamsSchema>;
 export type SessionsCompactionListResult = Static<typeof SessionsCompactionListResultSchema>;
 export type SessionsCompactionGetResult = Static<typeof SessionsCompactionGetResultSchema>;
 export type SessionsCompactionBranchResult = Static<typeof SessionsCompactionBranchResultSchema>;
 export type SessionsCompactionRestoreResult = Static<typeof SessionsCompactionRestoreResultSchema>;
+export type SessionsCheckpointCreateResult = Static<typeof SessionsCheckpointCreateResultSchema>;
 export type SessionsRewindParams = Static<typeof SessionsRewindParamsSchema>;
 export type SessionsForkParams = Static<typeof SessionsForkParamsSchema>;
 export type SessionsRewindResult = Static<typeof SessionsRewindResultSchema>;
diff --git a/packages/gateway-protocol/src/validator-registry.ts b/packages/gateway-protocol/src/validator-registry.ts
index 52848b12123..153584d02e9 100644
--- a/packages/gateway-protocol/src/validator-registry.ts
+++ b/packages/gateway-protocol/src/validator-registry.ts
@@ -233,6 +233,9 @@ export const validateSessionsCompactionBranchParams = compile(
 export const validateSessionsCompactionRestoreParams = compile(
   S.SessionsCompactionRestoreParamsSchema,
 );
+export const validateSessionsCheckpointCreateParams = compile(
+  S.SessionsCheckpointCreateParamsSchema,
+);
 export const validateSessionsBranchesListParams = compile(S.SessionsBranchesListParamsSchema);
 export const validateSessionsBranchesSwitchParams = compile(S.SessionsBranchesSwitchParamsSchema);
 export const validateSessionsRewindParams = compile(S.SessionsRewindParamsSchema);
diff --git a/src/gateway/methods/core-descriptors.ts b/src/gateway/methods/core-descriptors.ts
index 9b7596ea860..874304a51de 100644
--- a/src/gateway/methods/core-descriptors.ts
+++ b/src/gateway/methods/core-descriptors.ts
@@ -235,6 +235,7 @@ const CORE_GATEWAY_METHOD_SPECS = [
   ["sessions.compaction.get", "sessions-compaction-queries", "operator.read", "<=2026.7"],
   ["sessions.compaction.branch", "sessions-compaction-checkpoints", "operator.write", "<=2026.7"],
   ["sessions.compaction.restore", "sessions-compaction-checkpoints", "operator.admin", "<=2026.7"],
+  ["sessions.checkpoint.create", "sessions-compaction-checkpoints", "operator.admin", "2026.7"],
   ["sessions.branches.list", "sessions-rewind", "operator.read", "<=2026.7"],
   ["sessions.branches.switch", "sessions-rewind", "operator.admin", "<=2026.7"],
   ["sessions.rewind", "sessions-rewind", "operator.admin", "<=2026.7"],
diff --git a/src/gateway/server-methods/sessions-compaction-checkpoints.ts b/src/gateway/server-methods/sessions-compaction-checkpoints.ts
index 9591d488191..3f4414a4945 100644
--- a/src/gateway/server-methods/sessions-compaction-checkpoints.ts
+++ b/src/gateway/server-methods/sessions-compaction-checkpoints.ts
@@ -1,9 +1,10 @@
-// Compaction checkpoint branching and restore operations.
+// Compaction checkpoint creation, branching, and restore operations.
 import {
   ErrorCodes,
   errorShape,
   validateSessionsCompactionBranchParams,
   validateSessionsCompactionRestoreParams,
+  validateSessionsCheckpointCreateParams,
 } from "../../../packages/gateway-protocol/src/index.js";
 import { clearSessionQueues } from "../../auto-reply/reply/queue/cleanup.js";
 import { SESSION_LIFECYCLE_CHANGED_ERROR_REASON } from "../../config/sessions.js";
@@ -15,6 +16,7 @@ import {
 import {
   createFileBackedCompactionCheckpointStore,
   getSessionCompactionCheckpoint,
+  persistExplicitSessionCompactionCheckpoint,
 } from "../session-compaction-checkpoints.js";
 import { buildDashboardSessionKey } from "../session-create-service.js";
 import { resolveRequestedSessionAgentId as resolveRequestedGlobalAgentId } from "../session-request-agent.js";
@@ -35,6 +37,65 @@ const MODEL_SELECTION_LOCKED_CHECKPOINT_MESSAGE =
   "Checkpoint branch and restore are unavailable while model selection is locked.";
 
 export const sessionCheckpointHandlers: GatewayRequestHandlers = {
+  "sessions.checkpoint.create": async ({ params, respond, context }) => {
+    if (
+      !assertValidParams(
+        params,
+        validateSessionsCheckpointCreateParams,
+        "sessions.checkpoint.create",
+        respond,
+      )
+    ) {
+      return;
+    }
+    const key = requireSessionKey(params.key, respond);
+    if (!key) {
+      return;
+    }
+    const cfg = context.getRuntimeConfig();
+    const requestedAgent = resolveRequestedGlobalAgentId(cfg, key);
+    if (!requestedAgent.ok) {
+      respond(false, undefined, requestedAgent.error);
+      return;
+    }
+    const { entry, canonicalKey, target, storePath } = loadAccessorSessionEntryForGatewayTarget({
+      key,
+      cfg,
+      agentId: requestedAgent.agentId,
+    });
+    if (!entry?.sessionId) {
+      respond(
+        false,
+        undefined,
+        errorShape(ErrorCodes.INVALID_REQUEST, `session not found: ${key}`),
+      );
+      return;
+    }
+    const checkpoint = await persistExplicitSessionCompactionCheckpoint({
+      cfg,
+      sessionKey: canonicalKey,
+      sessionId: entry.sessionId,
+      agentId: target.agentId,
+      storePath,
+      checkpointId: params.checkpointId,
+      ...(params.summary?.trim() ? { summary: params.summary.trim() } : {}),
+      ...(typeof entry.totalTokens === "number" ? { tokensBefore: entry.totalTokens } : {}),
+    });
+    if (!checkpoint) {
+      respond(
+        false,
+        undefined,
+        errorShape(ErrorCodes.UNAVAILABLE, "failed to persist session checkpoint"),
+      );
+      return;
+    }
+    respond(
+      true,
+      { ok: true, key: canonicalKey, sessionId: entry.sessionId, checkpoint },
+      undefined,
+    );
+    emitSessionsChanged(context, { sessionKey: canonicalKey, reason: "checkpoint-create" });
+  },
   "sessions.compaction.branch": async ({ params, respond, context }) => {
     if (
       !assertValidParams(
diff --git a/src/gateway/server.sessions.compaction.test.ts b/src/gateway/server.sessions.compaction.test.ts
index d72495de519..a6f42bef9c0 100644
--- a/src/gateway/server.sessions.compaction.test.ts
+++ b/src/gateway/server.sessions.compaction.test.ts
@@ -232,6 +232,63 @@ async function alignCheckpointBoundaryWithSqliteRows(params: {
   }));
 }
 
+test("sessions.checkpoint.create persists the current SQLite transcript leaf", async () => {
+  const { storePath } = await createSessionStoreDir();
+  const sessionId = "session-explicit-checkpoint";
+  await seedSessionEntry({
+    entry: sessionStoreEntry(sessionId, { totalTokens: 321 }),
+    sessionKey: "agent:main:main",
+    storePath,
+  });
+  await seedTranscriptRows({
+    sessionId,
+    sessionKey: "agent:main:main",
+    storePath,
+    totalLines: 2,
+  });
+  const rows = await loadTranscriptRows({
+    sessionId,
+    sessionKey: "agent:main:main",
+    storePath,
+  });
+  const leafId = rows.toReversed().find((row) => row.type !== "session")?.id;
+  expect(typeof leafId).toBe("string");
+  const { ws } = await openClient();
+
+  const created = await rpcReq<{
+    ok: true;
+    key: string;
+    sessionId: string;
+    checkpoint: SessionCompactionCheckpoint;
+  }>(ws, "sessions.checkpoint.create", {
+    key: "main",
+    checkpointId: "manual-before-dispatch",
+    summary: "before customer-service dispatch",
+  });
+
+  expect(created.ok).toBe(true);
+  expect(created.payload).toMatchObject({
+    key: "agent:main:main",
+    sessionId,
+    checkpoint: {
+      checkpointId: "manual-before-dispatch",
+      summary: "before customer-service dispatch",
+      tokensBefore: 321,
+      preCompaction: { leafId, entryId: leafId },
+      postCompaction: { leafId, entryId: leafId },
+    },
+  });
+  const listed = await rpcReq<{ checkpoints: SessionCompactionCheckpoint[] }>(
+    ws,
+    "sessions.compaction.list",
+    { key: "main" },
+  );
+  expect(listed.payload?.checkpoints).toEqual([
+    expect.objectContaining({ checkpointId: "manual-before-dispatch" }),
+  ]);
+  ws.close();
+});
+
 test("sessions.compaction.* lists checkpoints and branches or restores from compacted transcripts", async () => {
   const { dir, storePath } = await createSessionStoreDir();
   const fixture = await createCheckpointFixture(dir, { legacyPreCompactionSnapshot: false });
diff --git a/src/gateway/session-compaction-checkpoints.ts b/src/gateway/session-compaction-checkpoints.ts
index b7c761ca6e3..8d5952fa167 100644
--- a/src/gateway/session-compaction-checkpoints.ts
+++ b/src/gateway/session-compaction-checkpoints.ts
@@ -89,6 +89,9 @@ type PersistSessionCompactionCheckpointParams = {
   cfg: OpenClawConfig;
   sessionKey: string;
   sessionId: string;
+  agentId?: string;
+  storePath?: string;
+  checkpointId?: string;
   reason: SessionCompactionCheckpointReason;
   snapshot: CapturedCompactionCheckpointSnapshot;
   summary?: string;
@@ -712,13 +715,20 @@ async function persistSessionCompactionCheckpoint(
     return null;
   }
 
-  const target = resolveGatewaySessionStoreTarget({
-    cfg: params.cfg,
-    key: params.sessionKey,
-  });
+  const target = params.storePath
+    ? {
+        agentId: params.agentId ?? "main",
+        storePath: params.storePath,
+        canonicalKey: params.sessionKey,
+      }
+    : resolveGatewaySessionStoreTarget({
+        cfg: params.cfg,
+        key: params.sessionKey,
+        ...(params.agentId ? { agentId: params.agentId } : {}),
+      });
   const createdAt = params.createdAt ?? Date.now();
   const checkpoint: SessionCompactionCheckpoint = {
-    checkpointId: randomUUID(),
+    checkpointId: params.checkpointId?.trim() || randomUUID(),
     sessionKey: target.canonicalKey,
     sessionId: params.sessionId,
     createdAt,
@@ -753,6 +763,7 @@ async function persistSessionCompactionCheckpoint(
   let stored = false;
   const updatedEntry = await updateSessionEntry(
     {
+      agentId: target.agentId,
       storePath: target.storePath,
       sessionKey: target.canonicalKey,
     },
@@ -787,6 +798,46 @@ async function persistSessionCompactionCheckpoint(
   return checkpoint;
 }
 
+export async function persistExplicitSessionCompactionCheckpoint(params: {
+  cfg: OpenClawConfig;
+  sessionKey: string;
+  sessionId: string;
+  agentId: string;
+  storePath: string;
+  checkpointId: string;
+  summary?: string;
+  tokensBefore?: number;
+}): Promise<SessionCompactionCheckpoint | null> {
+  const transcriptState = await readSessionLeafStateFromTranscriptAsync({
+    agentId: params.agentId,
+    sessionId: params.sessionId,
+    sessionKey: params.sessionKey,
+    storePath: params.storePath,
+  });
+  const leafId = transcriptState?.leafId;
+  if (!leafId) {
+    return null;
+  }
+  return await persistSessionCompactionCheckpoint({
+    cfg: params.cfg,
+    sessionKey: params.sessionKey,
+    sessionId: params.sessionId,
+    agentId: params.agentId,
+    storePath: params.storePath,
+    checkpointId: params.checkpointId,
+    reason: "manual",
+    snapshot: {
+      sessionId: params.sessionId,
+      leafId,
+      ...(transcriptState.entryId ? { entryId: transcriptState.entryId } : {}),
+    },
+    ...(params.summary ? { summary: params.summary } : {}),
+    ...(typeof params.tokensBefore === "number" ? { tokensBefore: params.tokensBefore } : {}),
+    postLeafId: leafId,
+    ...(transcriptState.entryId ? { postEntryId: transcriptState.entryId } : {}),
+  });
+}
+
 export function listSessionCompactionCheckpoints(
   entry: Pick<SessionEntry, "compactionCheckpoints"> | undefined,
 ): SessionCompactionCheckpoint[] {
-- 
2.50.1 (Apple Git-155)

