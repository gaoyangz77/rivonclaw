From faba200a3ef11235b0dbc8e19b321a4ceddf5c28 Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Sat, 8 Aug 2026 00:52:01 -0700
Subject: [PATCH] vendor(openclaw): expose silent completion for agent RPC

Why:
- Trusted internal RPC callers need clean empty model completions to settle as intentional silent replies.

Removal:
- Drop when upstream AgentParamsSchema and gateway execution expose allowEmptyAssistantReplyAsSilent without overriding subagent handoff behavior.

Tests:
- apps/desktop/src/cs-bridge/vendor-agent-silent-completion.sentinel.test.ts
---
 .../gateway-protocol/src/schema/agent.test.ts   | 17 +++++++++++++++++
 packages/gateway-protocol/src/schema/agent.ts   |  1 +
 .../command/attempt-execution.cli.test.ts       |  2 ++
 src/agents/command/attempt-execution.ts         |  5 ++++-
 src/agents/command/types.ts                     |  2 ++
 .../agent-turn/agent-run-execution-phase.ts     |  1 +
 .../server-methods/agent-request-types.ts       |  1 +
 .../agent.events-and-subagents.test-utils.ts    |  3 +++
 8 files changed, 31 insertions(+), 1 deletion(-)

diff --git a/packages/gateway-protocol/src/schema/agent.test.ts b/packages/gateway-protocol/src/schema/agent.test.ts
index f2d0eae9d03..9e26e4172b0 100644
--- a/packages/gateway-protocol/src/schema/agent.test.ts
+++ b/packages/gateway-protocol/src/schema/agent.test.ts
@@ -71,6 +71,23 @@ const musicCompletionEvent: AgentInternalEvent = {
 };
 
 describe("AgentParamsSchema", () => {
+  it("accepts only boolean silent empty-reply overrides", () => {
+    const params = makeAgentParamsWithInternalEvent(musicCompletionEvent);
+
+    expect(
+      Value.Check(AgentParamsSchema, {
+        ...params,
+        allowEmptyAssistantReplyAsSilent: true,
+      }),
+    ).toBe(true);
+    expect(
+      Value.Check(AgentParamsSchema, {
+        ...params,
+        allowEmptyAssistantReplyAsSilent: "true",
+      }),
+    ).toBe(false);
+  });
+
   it("accepts the backend expected-session binding", () => {
     expect(
       Value.Check(AgentParamsSchema, {
diff --git a/packages/gateway-protocol/src/schema/agent.ts b/packages/gateway-protocol/src/schema/agent.ts
index 2e687a75799..4dea0cf9c59 100644
--- a/packages/gateway-protocol/src/schema/agent.ts
+++ b/packages/gateway-protocol/src/schema/agent.ts
@@ -327,6 +327,7 @@ export const AgentParamsSchema = closedObject({
       Type.Literal("raw"),
     ]),
   ),
+  allowEmptyAssistantReplyAsSilent: Type.Optional(Type.Boolean()),
   extraSystemPrompt: Type.Optional(Type.String()),
   bootstrapContextMode: Type.Optional(
     Type.Union([Type.Literal("full"), Type.Literal("lightweight")]),
diff --git a/src/agents/command/attempt-execution.cli.test.ts b/src/agents/command/attempt-execution.cli.test.ts
index a75d253f96a..bc87ff35e5d 100644
--- a/src/agents/command/attempt-execution.cli.test.ts
+++ b/src/agents/command/attempt-execution.cli.test.ts
@@ -4245,6 +4245,7 @@ describe("CLI attempt execution", () => {
       opts: {
         modelRun: true,
         promptMode: "none",
+        allowEmptyAssistantReplyAsSilent: true,
         messageProvider: "discord-voice",
         inputProvenance: {
           kind: "inter_session",
@@ -4268,6 +4269,7 @@ describe("CLI attempt execution", () => {
       messageProvider: "discord-voice",
       modelRun: true,
       promptMode: "none",
+      allowEmptyAssistantReplyAsSilent: true,
       disableTools: true,
     });
     expect(firstEmbeddedAgentArg().prompt).not.toContain("[Inter-session message]");
diff --git a/src/agents/command/attempt-execution.ts b/src/agents/command/attempt-execution.ts
index 35ebb8dac2d..fc8235d1197 100644
--- a/src/agents/command/attempt-execution.ts
+++ b/src/agents/command/attempt-execution.ts
@@ -1405,7 +1405,10 @@ export function runAgentAttempt(params: {
     modelRun: params.opts.modelRun,
     promptMode: params.opts.promptMode,
     disableTools,
-    allowEmptyAssistantReplyAsSilent: isSubagentLane || isSubagentAnnounceHandoff,
+    allowEmptyAssistantReplyAsSilent:
+      params.opts.allowEmptyAssistantReplyAsSilent === true ||
+      isSubagentLane ||
+      isSubagentAnnounceHandoff,
     onAgentEvent: params.onAgentEvent,
     onExecutionPhase: onRuntimeActivity,
     deferTerminalLifecycle: params.deferTerminalLifecycle,
diff --git a/src/agents/command/types.ts b/src/agents/command/types.ts
index 6627dd94111..97b3f79a46f 100644
--- a/src/agents/command/types.ts
+++ b/src/agents/command/types.ts
@@ -234,6 +234,8 @@ export type AgentCommandOpts = {
   modelRun?: boolean;
   /** Internal prompt-mode override for trusted local/gateway callsites. */
   promptMode?: PromptMode;
+  /** Treat a clean empty assistant completion as an intentional silent reply. */
+  allowEmptyAssistantReplyAsSilent?: boolean;
   /** Internal ACP-ready session turn source. Manual spawn turns bypass only the dispatch gate. */
   acpTurnSource?: AcpTurnSource;
   /** Internal handoffs can feed the model without writing the synthetic prompt to transcript. */
diff --git a/src/gateway/agent-turn/agent-run-execution-phase.ts b/src/gateway/agent-turn/agent-run-execution-phase.ts
index a72ff2f4c16..08df4b21935 100644
--- a/src/gateway/agent-turn/agent-run-execution-phase.ts
+++ b/src/gateway/agent-turn/agent-run-execution-phase.ts
@@ -385,6 +385,7 @@ export function startAgentRunExecution(params: {
               lane: params.request.lane,
               modelRun: params.request.modelRun === true,
               promptMode: params.request.promptMode,
+              allowEmptyAssistantReplyAsSilent: params.request.allowEmptyAssistantReplyAsSilent,
               extraSystemPrompt: params.request.extraSystemPrompt,
               gitCoauthorAttribution: prepareGitCoauthorAttribution({
                 agentId: params.activeSessionAgentId,
diff --git a/src/gateway/server-methods/agent-request-types.ts b/src/gateway/server-methods/agent-request-types.ts
index 7dbe6270614..dfbbd833ec0 100644
--- a/src/gateway/server-methods/agent-request-types.ts
+++ b/src/gateway/server-methods/agent-request-types.ts
@@ -32,6 +32,7 @@ export type AgentRunRequest = {
   extraSystemPrompt?: string;
   modelRun?: boolean;
   promptMode?: "full" | "minimal" | "none" | "raw";
+  allowEmptyAssistantReplyAsSilent?: boolean;
   bootstrapContextMode?: "full" | "lightweight";
   bootstrapContextRunKind?: "default" | "heartbeat" | "cron";
   acpTurnSource?: "manual_spawn";
diff --git a/src/gateway/server-methods/agent.events-and-subagents.test-utils.ts b/src/gateway/server-methods/agent.events-and-subagents.test-utils.ts
index 80cc0e034dc..ae0f8429933 100644
--- a/src/gateway/server-methods/agent.events-and-subagents.test-utils.ts
+++ b/src/gateway/server-methods/agent.events-and-subagents.test-utils.ts
@@ -486,6 +486,7 @@ describe("gateway agent handler", () => {
         model: "llama3.2:latest",
         modelRun: true,
         promptMode: "none",
+        allowEmptyAssistantReplyAsSilent: true,
         sessionKey: "agent:main:main",
         inputProvenance: {
           kind: "inter_session",
@@ -504,11 +505,13 @@ describe("gateway agent handler", () => {
       message?: string;
       modelRun?: boolean;
       promptMode?: string;
+      allowEmptyAssistantReplyAsSilent?: boolean;
     }>();
     expectRecordFields(callArgs, {
       message: "Reply exactly: pong",
       modelRun: true,
       promptMode: "none",
+      allowEmptyAssistantReplyAsSilent: true,
     });
     expect(callArgs.message).not.toContain("[Inter-session message]");
 
-- 
2.50.1 (Apple Git-155)

