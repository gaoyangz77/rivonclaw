From 556a719da9abf66e2e465c722426eb63f3630cda Mon Sep 17 00:00:00 2001
From: RivonClaw CI <ci@rivonclaw.com>
Date: Sat, 8 Aug 2026 00:52:01 -0700
Subject: [PATCH] vendor(openclaw): rotate sessions after compaction failure

Why:
- Unrecoverable compaction failures otherwise leave later turns pinned to the same poisoned SQLite lifecycle.

Removal:
- Drop when upstream resets the durable session lifecycle after both thrown and payload-based compaction failures.

Tests:
- apps/desktop/src/gateway/vendor-compaction-session-recovery.sentinel.test.ts
---
 .../reply/agent-runner-error-handler.ts       | 14 +++++++++--
 src/auto-reply/reply/agent-runner-execute.ts  |  3 +++
 ...-runner-execution-context-failures.test.ts | 20 ++++++++++++----
 .../reply/agent-runner-execution.types.ts     |  1 +
 .../reply/agent-runner-fallback-settlement.ts | 14 +++++++++--
 src/auto-reply/reply/agent-runner-run.ts      | 21 ++++++++++++++++
 .../reply/followup-turn-execution.ts          | 24 +++++++++++++++++++
 7 files changed, 88 insertions(+), 9 deletions(-)

diff --git a/src/auto-reply/reply/agent-runner-error-handler.ts b/src/auto-reply/reply/agent-runner-error-handler.ts
index 4fb9a6791dc..46e08b0a151 100644
--- a/src/auto-reply/reply/agent-runner-error-handler.ts
+++ b/src/auto-reply/reply/agent-runner-error-handler.ts
@@ -219,8 +219,18 @@ export async function handleAgentExecutionError(params: {
   }
   if (isCompactionFailure) {
     takePendingLifecycleTerminal().emit("error", err);
+    let didReset = false;
+    try {
+      didReset = (await turn.resetSessionAfterCompactionFailure?.(message)) === true;
+    } catch (error) {
+      defaultRuntime.error(
+        `Failed to reset session after auto-compaction failure: ${formatErrorMessage(error)}`,
+      );
+    }
     defaultRuntime.error(
-      `Auto-compaction failed (${message}). Preserving existing session mapping for ${turn.sessionKey ?? turn.followupRun.run.sessionId}.`,
+      didReset
+        ? `Auto-compaction failed (${message}). Reset ${turn.sessionKey ?? turn.followupRun.run.sessionId} to a clean lifecycle.`
+        : `Auto-compaction failed (${message}). Preserving existing session mapping for ${turn.sessionKey ?? turn.followupRun.run.sessionId} because reset was unavailable.`,
     );
     turn.replyOperation?.fail("run_failed", err);
     return {
@@ -228,7 +238,7 @@ export async function handleAgentExecutionError(params: {
       payload: markAgentRunFailureReplyPayload({
         text: buildContextOverflowRecoveryText({
           duringCompaction: true,
-          preserveSessionMapping: true,
+          preserveSessionMapping: !didReset,
           cfg: params.runtimeConfig,
           agentId: turn.followupRun.run.agentId,
           primaryProvider: turn.followupRun.run.provider,
diff --git a/src/auto-reply/reply/agent-runner-execute.ts b/src/auto-reply/reply/agent-runner-execute.ts
index 0c4e88c8c16..21376c4c475 100644
--- a/src/auto-reply/reply/agent-runner-execute.ts
+++ b/src/auto-reply/reply/agent-runner-execute.ts
@@ -83,6 +83,7 @@ type ExecutePreparedReplyAgentRunInput = Pick<
   replyRouteThreadId: ReturnType<typeof resolveRoutedDeliveryThreadId>;
   replyToChannel: OriginatingChannelType | undefined;
   replyToMode: ReturnType<typeof resolveReplyToMode>;
+  resetSessionAfterCompactionFailure: (reason: string) => Promise<boolean>;
   resetSessionAfterRoleOrderingConflict: (reason: string) => Promise<boolean>;
   returnWithQueuedFollowupDrain: <T>(value: T) => T;
   runFollowupTurn: (queued: FollowupRun) => Promise<void>;
@@ -139,6 +140,7 @@ export async function executePreparedReplyAgentRun(
     replyThreadingOverride,
     replyToChannel,
     replyToMode,
+    resetSessionAfterCompactionFailure,
     resetSessionAfterRoleOrderingConflict,
     resolvedBlockStreamingBreak,
     resolvedQueue,
@@ -360,6 +362,7 @@ export async function executePreparedReplyAgentRun(
           shouldEmitToolResult,
           shouldEmitToolOutput,
           pendingToolTasks,
+          resetSessionAfterCompactionFailure,
           resetSessionAfterRoleOrderingConflict,
           isHeartbeat,
           sessionKey,
diff --git a/src/auto-reply/reply/agent-runner-execution-context-failures.test.ts b/src/auto-reply/reply/agent-runner-execution-context-failures.test.ts
index b6a60a463a5..b78660ae091 100644
--- a/src/auto-reply/reply/agent-runner-execution-context-failures.test.ts
+++ b/src/auto-reply/reply/agent-runner-execution-context-failures.test.ts
@@ -1,4 +1,4 @@
-import { describe, expect, it } from "vitest";
+import { describe, expect, it, vi } from "vitest";
 import { FailoverError } from "../../agents/failover-error.js";
 import type { SessionEntry } from "../../config/sessions.js";
 import { getReplyPayloadMetadata } from "../reply-payload.js";
@@ -21,7 +21,7 @@ import type { FallbackRunnerParams } from "./agent-runner-execution.test-support
 const state = await setupAgentRunnerExecutionTestState();
 
 describe("executeAgentTurn: context failures", () => {
-  it("preserves the active session when embedded overflow recovery fails", async () => {
+  it("resets the session lifecycle when embedded overflow recovery fails", async () => {
     state.isContextOverflowErrorMock.mockReturnValue(true);
     state.runEmbeddedAgentMock.mockResolvedValueOnce({
       payloads: [],
@@ -37,6 +37,7 @@ describe("executeAgentTurn: context failures", () => {
     const followupRun = createFollowupRun();
     followupRun.run.agentId = "main";
     const { replyOperation, failMock, updateSessionIdMock } = createMockReplyOperation();
+    const resetSessionAfterCompactionFailure = vi.fn(async () => true);
     const executeAgentTurn = await getExecuteAgentTurnForTest();
     const result = await executeAgentTurn({
       ...createMinimalRunAgentTurnParams({
@@ -51,11 +52,12 @@ describe("executeAgentTurn: context failures", () => {
       getActiveSessionEntry: () => activeSessionEntry,
       activeSessionStore,
       storePath: makeTestSessionStorePath(),
+      resetSessionAfterCompactionFailure,
     });
 
     expect(result.kind).toBe("final");
     if (result.kind === "final") {
-      expect(result.payload.text).toContain("kept this conversation mapped to the current session");
+      expect(result.payload.text).toContain("I've reset our conversation to start fresh");
       expect(result.payload.text).toContain("fresh session or using a model");
       expectRecordFields(requireRecord(getReplyPayloadMetadata(result.payload), "reply metadata"), {
         deliverDespiteSourceReplySuppression: true,
@@ -70,9 +72,12 @@ describe("executeAgentTurn: context failures", () => {
     expect(activeSessionStore["agent:main:main"]?.sessionId).toBe("session");
     expect(updateSessionIdMock).not.toHaveBeenCalled();
     expect(state.updateSessionStoreMock).not.toHaveBeenCalled();
+    expect(resetSessionAfterCompactionFailure).toHaveBeenCalledWith(
+      "400 The prompt is too long: 203557, model maximum context length: 196607",
+    );
   });
 
-  it("preserves the active session when compaction failure is thrown before reply", async () => {
+  it("resets the session lifecycle when compaction failure is thrown before reply", async () => {
     state.isCompactionFailureErrorMock.mockReturnValue(true);
     state.runEmbeddedAgentMock.mockRejectedValueOnce(
       new Error("Auto-compaction failed: nothing to compact"),
@@ -83,6 +88,7 @@ describe("executeAgentTurn: context failures", () => {
     const followupRun = createFollowupRun();
     followupRun.run.agentId = "main";
     const { replyOperation, failMock, updateSessionIdMock } = createMockReplyOperation();
+    const resetSessionAfterCompactionFailure = vi.fn(async () => true);
     const executeAgentTurn = await getExecuteAgentTurnForTest();
     const result = await executeAgentTurn({
       ...createMinimalRunAgentTurnParams({
@@ -97,11 +103,12 @@ describe("executeAgentTurn: context failures", () => {
       getActiveSessionEntry: () => activeSessionEntry,
       activeSessionStore,
       storePath: makeTestSessionStorePath(),
+      resetSessionAfterCompactionFailure,
     });
 
     expect(result.kind).toBe("final");
     if (result.kind === "final") {
-      expect(result.payload.text).toContain("kept this conversation mapped to the current session");
+      expect(result.payload.text).toContain("I've reset our conversation to start fresh");
       expect(result.payload.text).toContain("fresh session or using a model");
       expectRecordFields(requireRecord(getReplyPayloadMetadata(result.payload), "reply metadata"), {
         deliverDespiteSourceReplySuppression: true,
@@ -114,6 +121,9 @@ describe("executeAgentTurn: context failures", () => {
     expect(activeSessionStore["agent:main:main"]?.sessionId).toBe("session");
     expect(updateSessionIdMock).not.toHaveBeenCalled();
     expect(state.updateSessionStoreMock).not.toHaveBeenCalled();
+    expect(resetSessionAfterCompactionFailure).toHaveBeenCalledWith(
+      "Auto-compaction failed: nothing to compact",
+    );
   });
 
   it("uses structured FailoverError context_overflow over non-overflow message text", async () => {
diff --git a/src/auto-reply/reply/agent-runner-execution.types.ts b/src/auto-reply/reply/agent-runner-execution.types.ts
index ef57a8c2edb..61186b9573a 100644
--- a/src/auto-reply/reply/agent-runner-execution.types.ts
+++ b/src/auto-reply/reply/agent-runner-execution.types.ts
@@ -136,6 +136,7 @@ export type AgentTurnParams = {
   shouldEmitToolResult: () => boolean;
   shouldEmitToolOutput: () => boolean;
   pendingToolTasks: Set<Promise<void>>;
+  resetSessionAfterCompactionFailure?: (reason: string) => Promise<boolean>;
   resetSessionAfterRoleOrderingConflict: (reason: string) => Promise<boolean>;
   isHeartbeat: boolean;
   sessionKey?: string;
diff --git a/src/auto-reply/reply/agent-runner-fallback-settlement.ts b/src/auto-reply/reply/agent-runner-fallback-settlement.ts
index 44059e0f409..50ff5a4d78b 100644
--- a/src/auto-reply/reply/agent-runner-fallback-settlement.ts
+++ b/src/auto-reply/reply/agent-runner-fallback-settlement.ts
@@ -95,15 +95,25 @@ export async function settleAgentFallbackCycle(params: {
   };
   if (embeddedError && isContextOverflowError(embeddedError.message)) {
     emitSettledLifecycleError(new Error(terminalErrorMessage ?? "Agent run failed"));
+    let didReset = false;
+    try {
+      didReset = (await turn.resetSessionAfterCompactionFailure?.(embeddedError.message)) === true;
+    } catch (error) {
+      defaultRuntime.error(
+        `Failed to reset session after auto-compaction failure: ${formatErrorMessage(error)}`,
+      );
+    }
     defaultRuntime.error(
-      `Auto-compaction failed (${embeddedError.message}). Preserving existing session mapping for ${turn.sessionKey ?? turn.followupRun.run.sessionId}.`,
+      didReset
+        ? `Auto-compaction failed (${embeddedError.message}). Reset ${turn.sessionKey ?? turn.followupRun.run.sessionId} to a clean lifecycle.`
+        : `Auto-compaction failed (${embeddedError.message}). Preserving existing session mapping for ${turn.sessionKey ?? turn.followupRun.run.sessionId} because reset was unavailable.`,
     );
     turn.replyOperation?.fail("run_failed", embeddedError);
     return {
       kind: "final",
       payload: markAgentRunFailureReplyPayload({
         text: buildContextOverflowRecoveryText({
-          preserveSessionMapping: true,
+          preserveSessionMapping: !didReset,
           cfg: cycle.runtimeConfig,
           agentId: turn.followupRun.run.agentId,
           primaryProvider: turn.followupRun.run.provider,
diff --git a/src/auto-reply/reply/agent-runner-run.ts b/src/auto-reply/reply/agent-runner-run.ts
index 39ca64f0fae..0ef3d6e3de3 100644
--- a/src/auto-reply/reply/agent-runner-run.ts
+++ b/src/auto-reply/reply/agent-runner-run.ts
@@ -592,6 +592,26 @@ export async function runReplyAgent(
         activeIsNewSession = true;
       },
     });
+  const resetSessionAfterCompactionFailure = async (reason: string): Promise<boolean> =>
+    await resetReplyRunSession({
+      options: {
+        failureLabel: "compaction failure",
+        buildLogMessage: () =>
+          `Auto-compaction failed (${reason}). Reset session ${sessionKey} to a clean lifecycle; the failed transcript remains available.`,
+      },
+      sessionKey,
+      queueKey,
+      activeSessionEntry,
+      activeSessionStore,
+      storePath,
+      followupRun,
+      onActiveSessionEntry: (nextEntry) => {
+        activeSessionEntry = nextEntry;
+      },
+      onNewSession: () => {
+        activeIsNewSession = true;
+      },
+    });
   try {
     return await executePreparedReplyAgentRun({
       activeSessionStore,
@@ -620,6 +640,7 @@ export async function runReplyAgent(
       replyThreadingOverride,
       replyToChannel,
       replyToMode,
+      resetSessionAfterCompactionFailure,
       resetSessionAfterRoleOrderingConflict,
       resolvedBlockStreamingBreak,
       resolvedQueue,
diff --git a/src/auto-reply/reply/followup-turn-execution.ts b/src/auto-reply/reply/followup-turn-execution.ts
index 8bfc2194c92..ba0aa2f9d17 100644
--- a/src/auto-reply/reply/followup-turn-execution.ts
+++ b/src/auto-reply/reply/followup-turn-execution.ts
@@ -371,6 +371,30 @@ export async function executeFollowupTurn(params: {
           shouldEmitToolResult,
           shouldEmitToolOutput,
           pendingToolTasks,
+          resetSessionAfterCompactionFailure: async (reason) => {
+            const session = turn.session;
+            if (session.kind !== "session") {
+              return false;
+            }
+            return await resetReplyRunSession({
+              options: {
+                failureLabel: "compaction failure",
+                buildLogMessage: () =>
+                  `Auto-compaction failed (${reason}). Reset session ${session.key} to a clean lifecycle; the failed transcript remains available.`,
+              },
+              sessionKey: session.key,
+              queueKey: session.key,
+              activeSessionEntry: session.current(),
+              activeSessionStore: turn.sessionStore,
+              storePath: session.storePath,
+              followupRun: turn.queued,
+              onActiveSessionEntry: (entry) => {
+                session.adopt(entry);
+                turn.operation.updateSessionId(entry.sessionId);
+              },
+              onNewSession: () => undefined,
+            });
+          },
           resetSessionAfterRoleOrderingConflict: async (reason) => {
             const session = turn.session;
             if (session.kind !== "session") {
-- 
2.50.1 (Apple Git-155)

